# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование


### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- Пользователь может удалённо управлять отоплением в доме через веб-интерфейс.
- Система отправляет команды управления отоплением от сервера к модулю управления отоплением.
- Подключение модуля отопления выполняется только через выезд специалиста компании.
- Пользователь не может самостоятельно подключить новый датчик или реле к системе.

**Мониторинг температуры:**

- Пользователь может проверить текущую температуру в доме.
- Сервер получает данные о температуре синхронным запросом к датчику.
- Температурные данные хранятся в PostgreSQL.
- Система не поддерживает реактивное получение телеметрии от устройств.

### 2. Анализ архитектуры монолитного приложения

Текущее приложение реализовано как монолит на Go с единой базой данных PostgreSQL.

Основные особенности текущей архитектуры:

- Все функции находятся в одном монолитном приложении.
- Используется одна общая база данных PostgreSQL.
- Взаимодействие между сервером и устройствами полностью синхронное.
- Асинхронные вызовы, брокеры сообщений и реактивное взаимодействие отсутствуют.
- Управление устройствами инициируется сервером.
- Получение температуры также выполняется через запрос сервера к датчику.
- Подключение устройств не является self-service процессом и требует участия специалиста.
- Система поддерживает только управление отоплением и мониторинг температуры.


### 3. Определение доменов и границы контекстов

**Домен управления пользователями и домами**

- Контекст управления пользователями.
- Контекст управления домами и помещениями.
- Контекст прав доступа пользователей к домам и устройствам.

**Домен управления устройствами**

- Контекст регистрации и подключения устройств.
- Контекст управления состоянием устройств.
- Контекст интеграции с устройствами партнёров по стандартным протоколам.

**Домен управления отоплением**

- Контекст управления отопительными модулями.
- Контекст получения температурных данных.
- Контекст настройки правил отопления.

**Домен управления освещением**

- Контекст управления светом.
- Контекст сценариев включения и выключения освещения.

**Домен управления воротами**

- Контекст открытия и закрытия ворот.
- Контекст контроля состояния ворот.

**Домен видеонаблюдения**

- Контекст удалённого просмотра видеопотока.
- Контекст управления доступом к камерам.

**Домен сценариев автоматизации**

- Контекст создания пользовательских сценариев.
- Контекст выполнения сценариев на основе событий и расписаний.

**Домен телеметрии и событий**

- Контекст сбора телеметрии.
- Контекст хранения истории показаний.
- Контекст публикации событий от устройств.

### **4. Проблемы монолитного решения**

- Монолит сложно масштабировать по отдельным функциональным направлениям: отопление, свет, ворота, видеонаблюдение и сценарии развиваются с разной нагрузкой и разной скоростью.
- Единая кодовая база увеличивает риск при накатке новых версий: изменение логики отопления может повлиять на другие части системы.
- Отсутствует self-service подключение устройств, поэтому масштабирование на большое количество домов или изменения в логике работы существующего дома требует слишком много ручной работы специалистов.
- Синхронное взаимодействие с устройствами плохо подходит для телеметрии и событий умного дома, т.к. серверу приходится постоянно опрашивать устройства, это создаёт большую нагрузку на систему в целом. Кроме того, о каких-то важных собятиях, например, открытие ворот сервер узнает с задержкой и отреагирует позже, чем ожидается (например, включение света)
- Система не готова к подключению устройств партнёров и будущих неизвестных типов датчиков.
- Единая база данных создаёт жёсткую связанность между функциональными областями.
- Отсутствие асинхронной обработки усложняет сценарии автоматизации
- Команде из пяти разработчиков будет трудно параллельно развивать разные направления внутри одного монолита.


### 5. Визуализация контекста системы — диаграмма С4

[As-Is C4 Context Diagram](diagrams/context/as_is_context.puml)

# Задание 2. Проектирование микросервисной архитектуры

**Диаграмма контейнеров (Containers)**

[To-Be C4 Container Diagram](diagrams/container/to_be_container.puml)

**Диаграмма компонентов (Components)**

- [Device Management Service Components](diagrams/component/device_management_components.puml)
- [Heating Service Components](diagrams/component/heating_service_components.puml)
- [Lighting Service Components](diagrams/component/lighting_service_components.puml)
- [Gate Service Components](diagrams/component/gate_service_components.puml)
- [Video Surveillance Service Components](diagrams/component/video_service_components.puml)
- [Automation Service Components](diagrams/component/automation_service_components.puml)
- [Telemetry Service Components](diagrams/component/telemetry_service_components.puml)

**Диаграмма кода (Code)**

[Automation Rule Code Diagram](diagrams/code/automation_rule_code.puml)

# Задание 3. Разработка ER-диаграммы

[Smart Home ER Diagram](diagrams/er/smart_home_er.puml)

В диаграмме учтены: 
- пользователи
- дома и помещения
- производители устройств
- типы устройств и их возможности
- конкретные устройства в домах (инсталяции)
- телеметрия
- команды устройствам
- сценарии автоматизации

# Задание 4. Создание и документирование API

### 1. Тип API

Для целевой микросервисной архитектуры используется комбинированный подход:

- **REST API + OpenAPI** — для синхронных пользовательских операций: регистрация пользователей, управление домами, подключение устройств, создание сценариев, отправка команд управления отоплением, светом и воротами. Т.е. там, где мы ожидаем немедленный ответ
- **Event-driven API + AsyncAPI** — для асинхронных взаимодействий между микросервисами: получение телеметрии, изменение состояния устройств, публикация команд устройствам, срабатывание сценариев автоматизации.

Такой подход выбран потому, что пользовательские команды требуют понятного request/response интерфейса, а телеметрия и события умного дома являются потоками событий. Их лучше обрабатывать асинхронно через брокер сообщений. Это снижает связанность микросервисов и позволяет добавлять новые обработчики событий без изменения существующих сервисов. А также помогает невелировать проблемы с возможной пропажей связи и нестабильным соединением

### 2. Документация API

- [OpenAPI REST API specification](docs/api/openapi.yaml)
- [AsyncAPI Event API specification](docs/api/asyncapi.yaml)

# Задание 5. Работа с docker и docker-compose

Для выполнения задания добавлен отдельный сервис `temperature-api`.

## Что реализовано

- Создано приложение `temperature-api` на Go.
- Приложение предоставляет endpoint `/temperature?location=`.
- Также поддержан endpoint `/temperature/{sensorId}`, который используется существующим приложением `smart_home`.
- При каждом запросе сервис возвращает случайное значение температуры.
- Сервис упакован в Docker.
- В `docker-compose.yml` добавлены:
  - `temperature-api` на порту `8081`;
  - `postgres` со скриптом инициализации `./smart_home/init.sql`;
  - существующее приложение `smart_home`, использующее `TEMPERATURE_API_URL=http://temperature-api:8081`.

## Запуск

```bash
cd apps
docker compose up --build
```

## Проверка temperature-api

```bash
curl "http://localhost:8081/temperature?location=Living%20Room"
curl "http://localhost:8081/temperature/2"
```

## Проверка smart_home

```bash
curl http://localhost:8080/health
```

Также можно использовать Postman-коллекцию `smarthome-api.postman_collection.json` и выполнить:

- Create Sensor
- Get All Sensors

При повторных вызовах `Get All Sensors` значение температуры меняется



